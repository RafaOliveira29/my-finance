---
tipo: doc-projeto
projeto: MyFinance
stack: angular + dotnet + postgresql
arquitetura: Clean Architecture pragmática (CQRS-lite para leitura)
atualizado: 2026-08-12
---

# 📘 MyFinance — Documentação viva

> Controle financeiro pessoal orientado à **competência mensal** + **gestão de dívidas por credor**. Produto novo que **funde** dois projetos existentes (referência de domínio, não base de código). Nome provisório: **MyFinance** *(alternativas: Fôlego / Quitado / Saldo)*.

## 1. Visão
- **Ideia inicial:** um único app que responde "**quanto ainda posso gastar com segurança este mês**" e, no mesmo lugar, mostra "**o que devo, para quem, quanto, com que urgência, até quando, quanto paguei e quanto falta**" — com **visão em parcelas**, não só saldo total.
- **Estado final previsto:** MVP em 3 levas (núcleo mensal → dívidas/parcelas → conveniência), com auth real, dinheiro correto (`numeric`), integridade no banco e dashboard visual. Evoluções (metas, cartão, OFX, SaaS) no [[descoberta/08-features-futuras]].

## 2. Como começou
- **Brownfield / fusão.** Base de domínio vinda de:
  - **Projeto A** (`../Projeto A`) — .NET 8 Clean Arch + Angular 21 + **pacote de descoberta em 8 PDFs**. Domínio: competência mensal, renda/despesa, parcelamento, previsto×realizado, resumo. Código anêmico e sem auth, mas **doc excelente** e lógica de serviço boa.
  - **Projeto B** (`../Projeto B/finance-core`) — Next.js/React/shadcn gerado pelo v0.dev. Domínio: **dívidas/credores**, pagamentos de dívida, despesas avulsas, dashboard com gráficos, import CSV. Código de scaffold não revisado, mas **schema Postgres bom** e **UX/gráficos/import** úteis.
- **Nenhuma linha copiada.** Extraiu-se o **QUÊ** (domínio/requisitos/features) e reconstrói-se o **COMO** com qualidade. O estudo completo (entendimento, comparativo, dívidas a não repetir) está resumido no histórico e destilado no pacote de descoberta.

## 3. Estado atual
- ✅ **Etapa 1** — Estudo profundo dos dois projetos (leitura integral + 8 PDFs).
- ✅ **Etapa 2** — Pacote de descoberta reconciliado (8 artefatos, abaixo).
- ✅ **Etapa 3** — Stack/arquitetura travadas ([[descoberta/06-arquitetura]]).
- ✅ **Etapa 4** — Artefatos salvos no repo `my-finance/`.
- ✅ **Revisão adversarial concluída** (3 críticos: régua do cérebro, domínio/modelo, rastreabilidade) → veredito *aprovado com ressalvas*; **12 must-fix + 7 nice-to-fix aplicados** ao pacote (ver §5).
- ✅ **Etapa 5** — Plano em fases aprovado pelo dono (2026-07-17).
- ✅ **Fase 0 (fundação) concluída e verificada** — build 0 avisos/0 erros, 3 testes verdes, API sobe (`/health`, `/openapi/v1.json` 3.1.1), Postgres via docker + pipeline EF ok, Angular 21 zoneless buildando, CI configurado.
- ✅ **Fase 1 (Auth & tenant) concluída e verificada** — `User` (Argon2id) + JWT + `/register` `/login` `/me`; migration `users` (snake_case); seam `ICurrentUser` para multi-tenant; CORS. Front: login/registro/home, `authGuard`, interceptors (token + erro). **14 testes verdes**, smoke e2e (register→login→me + 401/409/400), front builda e serve.
- ✅ **Fase 2 (Cadastros base) concluída e verificada (2026-08-12)** — `Category`, `IncomeSource`, `ExpenseSource` (entidades ricas sobre `TenantEntity`), CRUD completo nas 3 rotas, isolamento multi-tenant no `EfRepository`, migration `AddCadastroBase`. Front: `CrudService` base parametrizado, 3 services, shell com navegação, 6 telas (lista + formulário), rotas protegidas, moeda/data em pt-BR. **Verificado:** 30 testes .NET + 14 testes Angular verdes; smoke de API com **39 checagens**; fluxo funcional **pela interface** em Chrome headless (criar→listar→editar→409 de categoria em uso→excluir→logout); **responsividade auditada em 7 larguras × 7 telas (320–1440), zero estouro horizontal**.
- ▶️ **Próxima: Fase 3 (Competência & pagamentos — o coração do dinheiro)**.

> **➡️ PONTO DE RETOMADA:** Fases 0, 1 e 2 concluídas e verificadas. **Próximo passo: Fase 3 — Competência & pagamentos** (`MonthlyEntry` + geração mensal idempotente + `PaymentService` com parcial/estorno/superpagamento; prova CA020–023, CA038–049, CA079). **Card vivo espelhado no cérebro:** `Cerebro/projetos/MyFinance/DOC.md` (regra [[documentacao-viva]]); este `DOC.md` do repo é a doc viva completa. **Projeto vive no repo git `my-finance`** (GitHub: `RafaOliveira29/my-finance`); Fase 2 **commitada localmente** em `3397de7`, **sem push** (Fases 0 e 1 já estão no GitHub com CI verde; o CI ainda não rodou para a Fase 2).

### Pacote de descoberta
1. [[descoberta/01-produto]] — PRD (visão, MVP, escopo in/out, RNFs mensuráveis, riscos).
2. [[descoberta/02-epicos]] — 14 épicos priorizados (MoSCoW + levas).
3. [[descoberta/03-historias]] — 60 HU + 10 HT unificadas (proveniência A/B/novo), em levas.
4. [[descoberta/04-criterios-aceite]] — 80 CAs (Gherkin, **IDs globais únicos**) + matriz de rastreabilidade + RF.
5. [[descoberta/05-modelagem-dominio]] — glossário, 10 entidades ricas, relacionamentos, regras de status, RN01–RN15, anti-patterns.
6. [[descoberta/06-arquitetura]] — stack travada + Clean pragmática/CQRS-lite (justificativa de performance).
7. [[descoberta/07-exemplo-uso]] — walkthrough (abril/2026) que valida o modelo, com dívida parcelada e à vista.
8. [[descoberta/08-features-futuras]] — roadmap + guarda de conceitos.

## 4. Arquitetura
**Clean Architecture pragmática** (4 camadas: Domain/Application/Infrastructure/API) + **CQRS-lite**: escrita pelo domínio rico + EF Core (transação, `RowVersion`); **leitura quente** (dashboard/resumo/painel de dívida) por **projeção SQL/Dapper com `GROUP BY` no Postgres**. Snapshot mensal materializado (`MonthlyEntry`). Detalhe e justificativa em [[descoberta/06-arquitetura]]. Front Angular 21 zoneless + signals + OnPush, contrato TS gerado do OpenAPI.

**Repo — monorepo simétrico** ([[organizacao-repositorio]]): `backend/` (`MyFinance.slnx`, `src/`, `tests/`, `global.json`) e `frontend/` como pastas irmãs; `descoberta/`, `.github/`, `docker-compose.yml`, `DOC.md`, `README.md` na raiz. CI único builda os dois.

## 5. Decisões
- **[2026-07-17] Nome** — opções: MyFinance / Fôlego / Quitado / Saldo · **escolhida (provisória):** MyFinance · **porquê:** aprovado pelo dono; renomear a pasta é trivial.
- **[2026-07-17] Stack** — **escolhida:** Angular 21+ · .NET (Clean) · PostgreSQL · **porquê:** decisão do dono; A já estava em Angular 21 e .NET Clean — herda a stack-alvo, descarta o código.
- **[2026-07-17] Estilo arquitetural** — opções: Layered / Clean / Clean+CQRS-full · **escolhida:** Clean pragmática + **CQRS-lite** · **porquê:** o caminho quente é leitura agregada; agregar no banco elimina o O(n·m) de A e as queries repetidas de B (RNF01/02) sem a cerimônia de CQRS-full.
- **[2026-07-17] Reconciliação de domínio** — **parcelamento migra para a Dívida** (`Debt` 1:1 `InstallmentPlan`); `Payment` único ancorado em `MonthlyEntry` (fonte única do realizado); despesa avulsa vira `MonthlyEntry Manual` (sem tabela `Expense`); renda preservada; discriminador `EntryType`+`SourceType`+CHECK. **porquê:** entrega a ênfase do dono (painel de dívida + parcelas) num só agregado e mata as dívidas dos dois modelos.
- **[2026-07-17] Não repetir** — auth real desde o dia 1, `numeric(18,2)` fim a fim, pagamento atômico, constraints no banco, zero feature decorativa, testes de dinheiro. Ver dívidas catalogadas no estudo (Etapa 1).
- **[2026-07-17] Revisão adversarial aplicada** — 3 críticos (régua/domínio/rastreabilidade) → *aprovado com ressalvas*; 12 must-fix + 7 nice-to-fix aplicados. Principais ajustes de modelagem (`05`): (a) **dívida à vista** projeta exatamente 1 competência no mês do `DueDate` (RN16, idempotente); (b) **`Debt.Status`** deriva do `InstallmentPlan.IsCompleted`, não das parcelas já materializadas; (c) origens com FK `RESTRICT`+soft-delete (não `SET NULL`, que colidia com o CHECK); (d) **pago/falta 100% derivados** (sem cache/coluna gravável — fecha a dupla-verdade); (e) **UNIQUE parciais por `SourceType`** para a idempotência; (f) **`AvailableToSpend`** define "já gasto" só como Manual (sem contagem dupla); (g) dimensão **`Nature`** materializada para as quebras; (h) **`Overdue` derivado** (não persistido), com índice por `DueDate`; (i) `ExpenseSource.DueMonthOffset` para competência ≠ vencimento; (j) política de superpagamento e ordenação de dívidas fixadas; + 5 CAs novos (CA076–CA080) e alinhamento de levas/links.
- **[2026-07-17] Monorepo simétrico** — reorganizado para `backend/` + `frontend/` irmãos (convenção [[organizacao-repositorio]]); `src/`, `tests/`, `MyFinance.slnx`, `global.json` movidos para `backend/`. Paths internos do `.slnx`/`.csproj` inalterados (relativos, moveram juntos); ajustados apenas o job backend do CI (`working-directory: backend`) e o README. `docker-compose.yml` (sem build context) e `.gitignore` (padrões não-ancorados) não precisaram mudar. Build/test/`npm start` revalidados verdes. **Sem git** (a pedido do dono).
- **[2026-07-17] Movido para o repositório git `my-finance`** — todo o projeto passou para o clone `projetos com claude/my-finance/` (GitHub `RafaOliveira29/my-finance`), preservando o `.git` do clone. A pasta é kebab (`my-finance`), mas os nomes internos seguem `MyFinance.*` (assemblies/namespaces) e `myfinance` (Angular) — independem do nome da pasta, então **nada quebrou**. O move inicial foi parcial (2 processos travando: a `MyFinance.API.exe` do smoke test e o `esbuild` do `ng serve`); recuperado matando os locks, movendo o fonte, reinstalando `frontend/node_modules` e removendo a `MyFinance/` antiga (com o `.git` acidental da Fase 0). Revalidado: **build 0/0, 3 testes ✔, `npm start` 200 + watch**. Sem commit/push (não solicitado).
- **[2026-07-17] Fase 0 — escolhas de fundação** — solution no formato **`.slnx`** (default do .NET 10); **Shouldly** em vez de FluentAssertions (v8+ exige licença paga); **`Microsoft.OpenApi` fixado em 2.11.0** para sanar a vuln **NU1903/GHSA-v5pm-xwqc-g5wc** da transitiva 2.0.0 (o v3 quebra o source generator do framework, então mantida a linha v2); **Angular zoneless explícito** (`provideZonelessChangeDetection`); Postgres exposto na porta **5433** (evita conflito com instância local); connection string dev só em `appsettings.Development.json` (prod via env `ConnectionStrings__Default`).
- **[2026-08-12] Fase 2 — isolamento multi-tenant no repositório, não no `HasQueryFilter`** — opções: (a) *global query filter* aplicado por reflexão no `OnModelCreating`; (b) *global query filter* declarado por entidade; (c) filtro explícito no `EfRepository`. **Escolhida: (c)**. **Porquê:** o EF **assa** o valor capturado do usuário no modelo cacheado — o filtro global fixava o `UserId` do primeiro request e vazava entre usuários; o repositório lê `ICurrentUser.UserId` **a cada consulta**, então o escopo acompanha a requisição. Efeito colateral bom: `GetById` fora do escopo devolve `null` → **404**, matando IDOR sem código extra. *(Nota do diagnóstico: depois de trocar para (c) ainda parecia haver vazamento; a inspeção do banco mostrou que **nenhuma** linha pertencia ao segundo usuário — ou seja, o que restava era **medição ruim do smoke antigo** (contar itens em vez de comparar ids) somada a dado residual e a processos `MyFinance.API` zumbis servindo build antigo. As três armadilhas estão registradas em `docs/COMO-TESTAR.md`.)*
- **[2026-08-12] Fase 2 — decisões menores** — `IRepository<T>` genérico (a Application não conhece EF) com `EfRepository<T>` escopando `TenantEntity`; **FK de categoria com `RESTRICT` + regra "categoria em uso → 409"** (a alternativa `SET NULL` colidiria com o CHECK de origem — ver §5/revisão adversarial); enums trafegam como **string** no JSON (`JsonStringEnumConverter`) para o contrato ficar legível; front usa **um `CrudService` base parametrizado** (os 3 serviços só declaram o recurso) e `apiErrorMessage` central para traduzir ProblemDetails; **`ExpenseKind` fixo em `Fixed`** na tela desta fase — despesa avulsa é `MonthlyEntry Manual` na Fase 3, então expor "Variável" agora criaria comportamento sem definição.
- **[2026-08-06] Fase 1 — auth & tenant** — Argon2id via **Isopoh.Cryptography.Argon2**; JWT HS256 (`System.IdentityModel.Tokens.Jwt` + `JwtBearer`); **snake_case** no Postgres via **EFCore.NamingConventions** aplicado no runtime **E** na factory de design-time (senão a migration divergiria do schema real); FluentValidation → `ValidationException` mapeada a **400** no handler global; `IUnitOfWork` = o próprio `DbContext` (mesma instância scoped); hasher/token singletons, repos scoped; `ICurrentUser` lê o `sub` do JWT. Front: interceptors funcionais + `authGuard` + rotas lazy. Chave JWT dev em `appsettings.Development.json` (prod via env `Jwt__SigningKey`).

## 6. Plano em fases (passo a passo) — onde paramos

> Sequência pensada para provar o motor de dinheiro cedo e entregar a ênfase de dívidas sem retrabalho. Cada fase termina **compilando, testada e verificável**, e ganha seu **Mapa de teste** em [docs/COMO-TESTAR.md](docs/COMO-TESTAR.md). Guia de uso do produto em [docs/MANUAL-DO-SISTEMA.md](docs/MANUAL-DO-SISTEMA.md).
>
> **Onde paramos:** Fases 0, 1 e 2 concluídas e verificadas → **próxima: Fase 3**.

- ✅ **Fase 0 — Fundação** (E14/HT) — **CONCLUÍDA (2026-07-17)**: solution `.slnx` .NET 10 (Domain/Application/Infrastructure/API + 2 de teste, refs Clean); EF Core 10 + Npgsql + Dapper; `MyFinanceDbContext` + factory de design-time; `GlobalExceptionHandler`→ProblemDetails; OpenAPI (`/openapi/v1.json`); healthcheck; base `Entity`/`AggregateRoot`; seams `AddApplication`/`AddInfrastructure`; docker-compose Postgres 16; Angular 21 **zoneless** em `frontend/`; CI GitHub Actions. **Verificado:** build 0/0, `dotnet test` 3✔, smoke API (`/health`+OpenAPI), EF `dbcontext info` + `db_ok=1`, front build ✔. **Prova:** CA071–CA073. *Obs.: geração de tipos TS do OpenAPI é ligada na Fase 1 (quando surgem os 1ºs endpoints), a partir do `/openapi/v1.json` já emitido.*
- ✅ **Fase 1 — Auth & tenant** (E1/HT09) — **CONCLUÍDA (2026-08-06)**: `User` rico + Argon2id + JWT HS256; `/api/auth/register|login|me`; migration `users` (snake_case, unique email); seam `ICurrentUser` (multi-tenant); `ValidationException`→400; CORS dev. Front: `AuthService` (signals+token), `authInterceptor` (Bearer), `errorInterceptor` (401→logout), `authGuard`, telas login/registro/home. **Verificado:** 14 testes ✔; smoke e2e register→login→me + 401(sem token/senha errada)/409(dup)/400(inválido); CORS 204 p/ `:4200`; front build + `npm start` 200. **Prova:** CA001–CA007. *Obs.: `HasQueryFilter` por `UserId` entra na Fase 2, junto das 1ªs entidades tenant.*
- ✅ **Fase 2 — Cadastros base** (E2/E3/E11 core) — **CONCLUÍDA (2026-08-12)**: escopo de tenant por `UserId` no `EfRepository` (não `HasQueryFilter` — ver §5); `Category` (cor/ícone, FK `RESTRICT`, "em uso → 409", inativar); `IncomeSource` (valor padrão, dia de competência, recorrência, vigência); `ExpenseSource` fixa (dia de vencimento + `DueMonthOffset` para competência ≠ vencimento); migration `AddCadastroBase` (`categories`, `income_sources`, `expense_sources`, snake_case, `numeric(18,2)`, enums como texto). Front: `CrudService` base parametrizado + 3 services, shell com navegação responsiva, 6 telas (lista/formulário), rotas protegidas, `apiErrorMessage` (ProblemDetails), locale pt-BR. **Verificado:** 30 testes .NET + 14 Angular; smoke de API com 39 checagens (CRUD, isolamento por id, IDOR 404, 409, 400, 401); fluxo funcional pela interface em Chrome headless; responsividade 320–1440 sem estouro. **Prova:** CA008–CA019, CA062–CA063, CA065, CA076.
- ⬜ **Fase 3 — Competência & pagamentos** (E7/E8/E4) — **PRÓXIMA**. *Coração do dinheiro*. Passos: 1) `MonthlyEntry` + `Nature` + CHECK de exatamente-uma-fonte + UNIQUE parciais; 2) `IMonthlyEntryGenerator` (geração idempotente do mês — RN04/RN10/RN16); 3) lançamento avulso (`SourceType=Manual`); 4) `Payment` + `IPaymentService` (parcial/total, guarda de superpagamento, estorno, status derivado, transação + RowVersion); 5) telas no front; 6) migration; 7) testes de dinheiro (RNF08) + Mapa de teste. **Prova:** CA020–CA023, CA038–CA049, CA079.
- ⬜ **Fase 4 — Resumo & dashboard** (E9/E10). Passos: 1) `IMonthlySummaryService` (Dapper/GROUP BY no Postgres); 2) "quanto posso gastar" (disponibilidade); 3) endpoints de resumo/stats; 4) dashboard no front (KPIs, gráficos por categoria/`Nature`, alertas); 5) verificação de performance (RNF01/02) + Mapa de teste. **Prova:** CA050–CA061.
- ⬜ **Fase 5 — Dívidas & parcelas (ênfase do dono)** (E5/E6). Passos: 1) `Creditor` (CRUD); 2) `Debt` (total, juros informativo, vencimento, prioridade, status derivado); 3) `InstallmentPlan` (valor da parcela + ajuste de centavos, projeção em `MonthlyEntry`); 4) dívida à vista (1 competência — RN16); 5) painel por credor (o quê/para quem/quanto/prioridade/vencimento/pago/falta) + visão em parcelas + priorização; 6) migration; 7) testes + Mapa de teste. **Prova:** CA024–CA037, CA064, CA077–CA078, CA080.
- ⬜ **Fase 6 — Conveniência** (E13/E12). Passos: 1) `Settings` por usuário efetivamente aplicadas (moeda/formato/forma padrão); 2) Import CSV (upload, mapeamento PT/EN, prévia, bulk/transação, parser robusto, erro por linha); 3) alertas de vencimento; 4) Mapa de teste. **Prova:** CA066–CA070.
- ⬜ **Fase 7 — Polimento**. Passos: 1) responsividade auditada 320–1440; 2) tema claro/escuro; 3) i18n pt-BR; 4) acessibilidade AA; 5) testes E2E dos fluxos-chave; 6) verificação de performance final (RNF01/02) + Mapa de teste. **Prova:** CA074–CA075.

> **Cobertura:** as 8 fases (Fase 0–7) cobrem os **80 CAs** (CA001–CA080). **Gate de qualidade:** a Fase 3 (coração do dinheiro) só inicia com o `05` já refletindo os ajustes da revisão (geração de dívida à vista/parcelada, status derivado, UNIQUE parciais, sem dupla-verdade) — já aplicados.

- [x] Fase 0 (fundação) **concluída e verificada**.
- [x] Fase 1 (Auth & tenant) **concluída e verificada**.
- [x] Fase 2 (Cadastros base) **concluída e verificada**.
- [ ] **Fase 3 — Competência & pagamentos** (próxima).

## 7. Documentação viva (onde mora)
- **Fonte da verdade (repo):** este `DOC.md` + `descoberta/01..08` (descoberta) + [`docs/COMO-TESTAR.md`](docs/COMO-TESTAR.md) (guia de testes, mapa por fase) + [`docs/MANUAL-DO-SISTEMA.md`](docs/MANUAL-DO-SISTEMA.md) (tutorial de uso) — versionados com o código.
- **Card vivo (cérebro):** `Cerebro/projetos/MyFinance/DOC.md` (roadmap de fases + estado) e `Cerebro/projetos/MyFinance/manual-do-sistema.md` (resumo do manual) — curtos, apontam para o repo; atualizados ao fim de cada bloco (regra [[documentacao-viva]]).
- **Handoff por fase (cérebro, fora do repo):** `Cerebro/projetos/MyFinance/handoffs/FASE-<n>-<slug>.md` — a **memória da jornada** de cada fase (decisões descartadas, becos sem saída, bugs que não eram bugs, armadilhas de ambiente), escrita a partir de `Cerebro/_meta/TEMPLATE-handoff-fase.md`. Começa na **Fase 2**. Este `DOC.md` diz *o que o sistema é hoje*; o handoff diz *como se chegou aqui*. As pastas de código ficam limpas — scripts de verificação pontual também moram no cérebro (`Cerebro/snippets/`).

## 8. Pilares aplicados
Segue os pilares compartilhados: [[persona-dev]], [[principios-codigo]], [[responsividade-e-ux]], [[documentacao-viva]].
